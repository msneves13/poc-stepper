import { SedsStepperNextDirective } from './seds-stepper/seds-stepper-next.directive';

describe('SedsStepperNextDirective', () => {
  it('should create an instance', () => {
    const directive = new SedsStepperNextDirective();
    expect(directive).toBeTruthy();
  });
});
