import { SedsStepperPreviousDirective } from './seds-stepper/seds-stepper-previous.directive';

describe('SedsStepperPreviousDirective', () => {
  it('should create an instance', () => {
    const directive = new SedsStepperPreviousDirective();
    expect(directive).toBeTruthy();
  });
});
